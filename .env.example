DATABASE_URL="postgresql://user:password@localhost:5432/slab_ledger?schema=public"

# Generate with: npx auth secret
AUTH_SECRET="replace-with-a-random-secret"

# Optional — only needed for the "Compare with AI" listing-photo check on a
# card's page. Without it, everything else in the app still works.
ANTHROPIC_API_KEY="sk-ant-..."
